可以。现在我们把你的学习路线从“工具清单”升级成一套**以就业为目标、以你刚刚做好的 `mall-admin-test` 为核心项目、最终能写进简历的测试工程师学习路线**。

> **课时笔记与进度（分门别类、随学随记）请看：**  
> [`doc/learning/README.md`](./learning/README.md) — 总目录与十阶段索引  
> **AI 续课记忆**：[`doc/learning/记忆.md`](./learning/记忆.md)（新对话请 `@` 引用）  
> 进度打卡：[`doc/learning/progress.md`](./learning/progress.md)  
> 当前重点：阶段 3 Postman 收尾 → 阶段 4 Python → 阶段 5 Pytest

你的目标不是“把 Postman、Pytest、Selenium、JMeter、MeterSphere、Jenkins 都点一遍”，而是最终形成：

> **测试理论 → 手工测试 → 接口测试 → Python → Pytest → 接口自动化 → UI 自动化 → 性能测试 → 测试平台 → CI/CD → 项目经验 → 简历/面试**

---

# 一、先确定你的最终目标

你现在学习的目标，我建议定位为：

> **初级/中级测试开发方向的测试工程师**

而不是单纯的：

> “只会点点点的功能测试”。

你的技术栈最终应该形成：

```text
                 测试工程师
                     │
        ┌────────────┴────────────┐
        ↓                         ↓
     测试理论                    编程能力
        │                         │
   测试用例/缺陷                  Python
   测试流程/方法                   │
        │                         ↓
        ↓                       Pytest
     功能测试                      │
        │                         ↓
        └──────────────→ 接口自动化
                              │
                              ↓
                           Selenium
                              │
                              ↓
                           UI自动化
                              │
                              ↓
                            JMeter
                              │
                              ↓
                           性能测试
                              │
                              ↓
                         MeterSphere
                              │
                              ↓
                           Jenkins
                              │
                              ↓
                         CI/CD自动化
```

而你现在已经有一个非常关键的东西：

```text
mall-admin-test
```

它不是你的“学习成果展示网页”这么简单。

它应该成为你整个学习过程中的：

> **被测系统 SUT**

也就是说，接下来你学的每一个工具，都尽量拿这个项目来练。

---

# 二、你的整个学习路线

我给你分成 **10 个阶段**。

| 阶段 | 学什么               | 最终产出      |
| -- | ----------------- | --------- |
| 0  | 测试环境与项目理解         | 能独立运行项目   |
| 1  | 软件测试基础            | 能设计测试用例   |
| 2  | Web 功能测试          | 完整测试一个模块  |
| 3  | Postman 接口测试      | 接口测试集合    |
| 4  | Python            | 能写测试脚本    |
| 5  | Pytest + Requests | 接口自动化框架   |
| 6  | Selenium          | UI 自动化框架  |
| 7  | JMeter            | 性能测试项目    |
| 8  | MeterSphere       | 企业测试平台    |
| 9  | Jenkins           | CI/CD 自动化 |
| 10 | 项目包装 + 面试         | 简历 + 面试   |

**顺序不要乱。**

尤其不要：

> 今天 Selenium，明天 JMeter，后天 Jenkins。

这样很容易变成“每个工具都会一点，但什么都不会”。

---

# 三、阶段 0：先把你的项目彻底搞懂

你现在已经完成：

```text
Spring Boot
+
Vue
+
MySQL
+
JWT
+
Swagger
+
Docker
+
日志
+
权限
+
购物车
+
订单
+
库存
```

所以现在第一件事不是马上写代码。

而是：

> **把这个系统当成你入职公司后接手的项目。**

---

## 0.1 你需要理解系统架构

你至少要知道：

```text
浏览器
   │
   ↓
Vue
   │
   ↓
Axios
   │
   ↓
HTTP
   │
   ↓
Spring Boot
   │
   ├── Controller
   ├── Service
   ├── Mapper
   │
   ↓
MySQL
```

同时：

```text
用户
 ↓
登录
 ↓
JWT
 ↓
接口
 ↓
权限校验
 ↓
业务逻辑
 ↓
数据库
```

你以后面试一定会遇到：

> “你测试的项目架构是什么？”

你不能回答：

> “就是一个 Spring Boot 项目。”

你应该能够讲清楚：

> 前端采用 Vue，后端采用 Spring Boot，通过 RESTful API 通信，MySQL 负责数据存储，登录采用 JWT 鉴权，后台根据角色进行权限控制……

这才是项目经验。

---

# 四、阶段 1：测试基础

这一阶段非常重要。

很多初学者的问题是：

> 工具会了，但是不知道怎么测。

所以先学测试思想。

---

## 1.1 软件测试生命周期

你要理解：

```text
需求
 ↓
测试计划
 ↓
测试设计
 ↓
测试用例
 ↓
测试执行
 ↓
缺陷管理
 ↓
回归测试
 ↓
测试报告
 ↓
上线
```

你以后面试：

> “你们公司的测试流程是什么？”

就可以回答。

---

# 五、测试用例

你需要掌握：

### 等价类

例如：

```text
年龄：
0~17
18~60
61+
```

不是每个数字都测试。

而是：

```text
有效等价类
无效等价类
```

---

## 5.1 边界值

例如：

```text
库存 >= 0
```

那么测试：

```text
-1
0
1
```

例如：

```text
商品价格 > 0
```

测试：

```text
-1
0
0.01
1
```

---

## 5.2 场景法

例如订单：

```text
登录
 ↓
浏览商品
 ↓
加入购物车
 ↓
创建订单
 ↓
支付
 ↓
发货
 ↓
完成
```

不仅测试每个接口，还要测试完整业务链路。

---

## 5.3 判定表

例如：

```text
商品存在？
商品上架？
库存足够？
用户登录？
```

最终决定：

```text
能不能购买？
```

这就是很典型的测试设计。

---

# 六、阶段 2：Web 功能测试

然后正式拿你的项目测试。

你需要测试：

```text
登录
用户
分类
商品
库存
购物车
订单
```

---

## 6.1 第一份真正的测试用例

例如：

### 登录

| 用例    | 用户名      | 密码  | 预期      |
| ----- | -------- | --- | ------- |
| 正常    | admin    | 正确  | 登录成功    |
| 密码错误  | admin    | 错误  | 401     |
| 用户不存在 | xxx      | xxx | 401/404 |
| 空用户名  | 空        | xxx | 400     |
| 空密码   | admin    | 空   | 400     |
| 禁用用户  | disabled | 正确  | 失败      |

---

## 6.2 缺陷思维

你需要学会写 Bug：

```text
标题：
商品价格输入 -1 后仍可以创建商品

环境：
Chrome / Windows

前置条件：
管理员已登录

步骤：
1. 商品管理
2. 点击新增
3. 输入价格 -1
4. 点击保存

实际结果：
商品创建成功

预期结果：
提示价格必须大于 0

严重程度：
Major
```

这才是真正的测试工作。

---

# 七、阶段 3：Postman

这是你第一个真正的自动化测试工具。

你要从：

> “会发送请求”

进阶到：

> **“会设计接口测试。”**

---

## 7.1 第一阶段 Postman

学习：

```text
GET
POST
PUT
DELETE
```

然后：

```text
Headers
Query Params
Path Params
Body
JSON
Authorization
Bearer Token
```

---

# 八、接口测试核心

你的项目特别适合练：

```text
POST /api/auth/login
```

登录后获得：

```text
JWT
```

然后：

```text
Authorization: Bearer xxx
```

访问：

```text
/api/products
/api/inventory
/api/orders
```

---

# 九、接口关联

这是非常重要的。

例如：

```text
登录
 ↓
得到 token
 ↓
创建商品
 ↓
得到 productId
 ↓
增加库存
 ↓
创建购物车
 ↓
创建订单
 ↓
得到 orderId
 ↓
取消订单
```

这就是：

> **接口关联**

你以后面试说：

> “我使用 Postman 对业务接口进行了关联测试。”

就有实际项目可以支撑。

---

# 十、Postman 最终目标

最终你应该做出：

```text
Mall Test API
│
├── Auth
│   ├── Login
│   └── Logout
│
├── User
│
├── Category
│
├── Product
│
├── Inventory
│
├── Cart
│
└── Order
```

并且每个接口都有：

```text
正常
异常
边界
权限
数据校验
```

---

# 十一、阶段 4：Python

这时候才正式学 Python。

但你**不是为了转 Python 开发**。

你的目标是：

> **用 Python 写测试。**

---

## 11.1 Python 学习范围

不用一开始学：

```text
Django
Flask
异步编程
爬虫
机器学习
```

你现在重点：

```text
变量
字符串
数字
列表
字典
元组
集合
if
for
while
函数
异常
文件
JSON
模块
类
面向对象基础
```

---

# 十二、Python + HTTP

然后学习：

```python
requests
```

例如：

```python
response = requests.post(
    url,
    json=data
)

assert response.status_code == 200
```

你会发现：

> Postman 做的事情，可以开始用代码做了。

---

# 十三、阶段 5：Pytest

这是整个路线的**核心阶段**。

你最终简历里非常值得出现：

> Python + Pytest + Requests 接口自动化测试框架

---

## 13.1 从最简单开始

例如：

```text
test_login.py
```

测试：

```text
正确登录
错误密码
不存在用户
禁用用户
```

---

# 十四、Pytest 学习路线

依次：

```text
Pytest 基础
 ↓
assert
 ↓
fixture
 ↓
参数化
 ↓
conftest.py
 ↓
测试数据
 ↓
接口关联
 ↓
日志
 ↓
报告
 ↓
失败重试
 ↓
框架封装
```

---

# 十五、最终 Pytest 项目

最终形成：

```text
api-test/
│
├── config/
├── common/
├── utils/
├── api/
├── data/
├── tests/
│   ├── test_auth.py
│   ├── test_product.py
│   ├── test_inventory.py
│   ├── test_cart.py
│   └── test_order.py
│
├── conftest.py
├── pytest.ini
└── requirements.txt
```

然后：

```text
pytest
```

一键执行。

这时你才真正进入：

> **接口自动化测试工程师**

---

# 十六、阶段 6：Selenium

然后开始 UI 自动化。

你的项目已经专门为 Selenium 做了：

```text
data-testid
```

所以非常合适。

---

## 16.1 Selenium 学什么

先学：

```text
浏览器启动
元素定位
点击
输入
获取文本
等待
页面跳转
截图
```

然后：

```text
XPath
CSS Selector
显式等待
Page Object Model
```

---

# 十七、Selenium 不要一上来就做“大框架”

先写：

```text
test_login.py
```

实现：

```text
打开登录页
 ↓
输入账号
 ↓
输入密码
 ↓
点击登录
 ↓
验证 Dashboard
```

然后：

```text
test_product.py
```

再：

```text
test_order.py
```

最后才封装：

```text
Page Object
BasePage
DriverManager
Fixture
```

---

# 十八、阶段 7：JMeter

这时候你已经会：

```text
功能测试
接口测试
接口自动化
UI 自动化
```

才开始性能测试。

---

## 18.1 JMeter 基础

学习：

```text
Test Plan
 ↓
Thread Group
 ↓
HTTP Request
 ↓
HTTP Header
 ↓
CSV Data Set
 ↓
Assertions
 ↓
Listener
```

---

# 十九、性能测试核心指标

你必须理解：

### 并发用户

同时有多少用户请求。

### TPS

每秒处理多少事务。

### 响应时间

请求从发出到收到响应需要多久。

### 吞吐量

系统单位时间处理的数据量。

### 错误率

```text
失败请求 / 总请求
```

---

# 二十、用你的项目做性能测试

例如：

```text
GET /api/products
```

设计：

```text
10 users
50 users
100 users
500 users
```

观察：

```text
平均响应时间
P90
P95
P99
TPS
错误率
```

这时候你就不是“会用 JMeter”。

而是：

> **做过性能测试。**

---

# 二十一、阶段 8：MeterSphere

这个时候再学 MeterSphere。

它不是替代 Postman/Pytest。

而是让你理解：

> **企业测试平台是怎么组织测试工作的。**

学习：

```text
测试用例
 ↓
接口测试
 ↓
场景
 ↓
自动化
 ↓
性能测试
 ↓
测试报告
```

---

# 二十二、阶段 9：Jenkins

最后学 Jenkins。

你要实现：

```text
Git
 ↓
Jenkins
 ↓
拉代码
 ↓
安装依赖
 ↓
启动环境
 ↓
执行 Pytest
 ↓
生成报告
 ↓
构建结果
```

最终：

> **代码提交 → 自动测试**

这就是 CI/CD。

---

# 二十三、阶段 10：简历项目

最终你的简历项目就可以写成：

### 项目：电商后台测试平台

**技术栈：**

```text
Spring Boot
Vue 3
MySQL
JWT
Postman
Python
Pytest
Requests
Selenium
JMeter
MeterSphere
Jenkins
```

但注意：

**不要把没真正学会的东西写上去。**

我们会随着你的学习逐个打勾。

---

# 二十四、最终你应该拥有 4 个成果

你的学习最终不要只剩：

> “我看完了 XX 视频。”

而是要留下四套东西。

### ① 手工测试

```text
测试计划
测试用例
Bug
测试报告
```

### ② Postman

```text
API Collection
环境变量
接口断言
业务流程
```

### ③ Pytest

```text
接口自动化框架
测试代码
报告
```

### ④ Selenium

```text
UI 自动化框架
Page Object
测试报告
```

再加：

```text
JMeter 性能测试报告
MeterSphere 测试项目
Jenkins CI 流程
```

---

# 二十五、最终能力树

你学完以后，应该达到：

```text
测试工程师
│
├── 测试理论
│   ├── 等价类
│   ├── 边界值
│   ├── 场景法
│   ├── 判定表
│   └── 测试流程
│
├── 功能测试
│   ├── Web
│   ├── 用例
│   ├── Bug
│   └── 回归
│
├── 接口测试
│   ├── HTTP
│   ├── REST API
│   ├── Postman
│   ├── JSON
│   └── JWT
│
├── Python
│   ├── 基础语法
│   ├── 函数
│   ├── OOP
│   └── Requests
│
├── 接口自动化
│   ├── Pytest
│   ├── Fixture
│   ├── 参数化
│   ├── 断言
│   ├── 日志
│   └── 报告
│
├── UI 自动化
│   ├── Selenium
│   ├── XPath/CSS
│   ├── Wait
│   └── Page Object
│
├── 性能测试
│   ├── JMeter
│   ├── 并发
│   ├── TPS
│   ├── 响应时间
│   └── 性能报告
│
├── 测试平台
│   └── MeterSphere
│
└── CI/CD
    └── Jenkins
```

---

# 二十六、最重要的一点：你的学习方式

以后我们不要采用：

> **“你告诉我下一步学什么，我去看教程。”**

而采用：

> **“学一个知识 → 在 mall-admin-test 上实际做 → 产生测试资产 → 总结 → 面试表达。”**

例如今天学**等价类**：

```text
知识
 ↓
商品价格
 ↓
设计等价类
 ↓
写测试用例
 ↓
Postman 执行
 ↓
发现 Bug
 ↓
写 Bug
 ↓
总结
```

学完边界值：

```text
库存 >= 0
 ↓
-1 / 0 / 1
 ↓
Postman
 ↓
断言
 ↓
记录结果
```

学 Pytest：

```text
Postman 测试
 ↓
Python 重写
 ↓
Pytest
 ↓
参数化
 ↓
Fixture
 ↓
自动运行
```

这才是**真正有效的测试工程师学习路线**。

---

# 二十七、我们接下来的第一站

你现在**不要碰 Selenium、JMeter、MeterSphere、Jenkins**。

甚至暂时也不要急着写 Pytest。

我们从最基础但最重要的地方开始：

> **第一课：测试用例设计 + 等价类 + 边界值。**

然后直接拿你现在的 `mall-admin-test`：

```text
商品管理
```

作为第一个练习模块。

我们会从：

**“商品创建接口到底应该怎么测？”**

开始，一步一步写出第一份真正的测试用例。

这样后面你学 Postman、Pytest 时，每一个工具都有东西可自动化，而不是为了学工具而学工具。
