[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short

markers =
    smoke: 冒烟用例（pytest -m smoke）

# HTML 报告（可选）：pytest --html=htmlreport/report.html --self-contained-html
